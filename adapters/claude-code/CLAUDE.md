# Family Repurchase Agent Java Rules

本项目是 Java / Spring Boot 版家庭复购品价格决策 Agent Capability。

规则：

- 默认中文输出；
- 不提交真实订单数据；
- 修改 core 后运行 `mvn test`；
- 不直接绕过 Service / Repository 操作数据库；
- 不把 Agent 设计写成“完整独立 Agent Runtime”。
